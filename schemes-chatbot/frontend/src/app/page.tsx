"use client"

import React from "react"
import ChatPage from "@/components/ChatPage"

export default function Home() {
  // Start directly with chat interface - no business profile form needed
  return <ChatPage />
}